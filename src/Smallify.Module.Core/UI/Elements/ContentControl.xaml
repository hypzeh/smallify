<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="SmallifyIconControlStyle"
           BasedOn="{StaticResource {x:Type ContentControl}}"
           TargetType="{x:Type ContentControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ContentControl}">
                    <Viewbox>
                        <Canvas Width="28"
                                Height="28">
                            <Path Data="{StaticResource SmallifyCircle}"
                                  Fill="#1ed760" />
                            <Path Data="{StaticResource SmallifyStripes}"
                                  Fill="#000000" />
                            <Path Data="{StaticResource SmallifyMagnifyingGlass}"
                                  Stroke="#000000"
                                  StrokeEndLineCap="Round"
                                  StrokeStartLineCap="Round"
                                  StrokeThickness="2" />
                        </Canvas>
                    </Viewbox>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
