<UserControl
    x:Class="Smallify.Module.Settings.Views.AuthenticationSectionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    d:DesignHeight="450"
    d:DesignWidth="800"
    prism:ViewModelLocator.AutoWireViewModel="True"
    x:ClassModifier="internal"
    mc:Ignorable="d">

    <Grid>
        <StackPanel>
            <TextBlock
                FontSize="24"
                Text="Authentication" />
            <Button Command="{Binding Path=GetAuthenticationCodeCommand, Mode=OneWay}">
                <TextBlock Text="Get Code" />
            </Button>
            <StackPanel>
                <Label
                    Content="_Authorisation Code:"
                    Target="{Binding ElementName=authorisationCode}" />
                <TextBox
                    Name="authorisationCode"
                    Text="{Binding Path=AuthorisationCode, Mode=TwoWay}" />
            </StackPanel>

            <TextBlock Text="{Binding Path=ClientID, Mode=OneWay, StringFormat='Client ID: {0}'}" />
            <TextBlock Text="{Binding Path=ClientSecret, Mode=OneWay, StringFormat='Client Secret: {0}'}" />
            <TextBlock Text="{Binding Path=DisplayName, Mode=OneWay, StringFormat='Display Name: {0}'}" />
            <TextBlock Text="{Binding Path=Username, Mode=OneWay, StringFormat='Username: {0}'}" />

        </StackPanel>
    </Grid>
</UserControl>
